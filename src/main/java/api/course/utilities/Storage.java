package api.course.utilities;

import api.course.models.User;

import java.util.ArrayList;
import java.util.List;

// Storage represents static data storage: cache, hard disk, server, database, cloud, etc.
public class Storage {

//    private static final List<User> storage = UserGenerator.generateUserList(3);
//
//    private Storage() {
//        /* No instantiation. */
//    }
//
//    public static User getUser(int id) {
//
//        if (id < 0 || id >= storage.size()) {
//            throw new IllegalArgumentException("Non-existing id.");
//        }
//        if(!storage.get(id).isActive()) {
//            throw new IllegalArgumentException("User is not active.");
//        }
//
//        return storage.get(id);
//    }
//
//    public static List<User> getUsers(  ) {
//
//        return storage;
//    }
}
